'use strict';

describe('PTF.view1 module', function() {

  beforeEach(module('PTF.view1'));

  describe('PTF controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});